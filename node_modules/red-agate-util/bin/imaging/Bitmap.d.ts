export declare class Bitmap {
    private w;
    private h;
    private widthAligned;
    private buf;
    constructor(width: number, height: number, src?: Uint8Array);
    get width(): number;
    get height(): number;
    toBytes(): Uint8Array;
    set(x: number, y: number, v: number): void;
    get(x: number, y: number): number;
    clear(x: number, y: number, width: number, height: number): Bitmap;
    fill(x: number, y: number, width: number, height: number, v: number): Bitmap;
    clearAll(): Bitmap;
}
