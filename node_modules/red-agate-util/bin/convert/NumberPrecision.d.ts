/**
 * Number precision / significant figure
 */
export declare class NumberPrecision {
    static decimalPlaces(n: number): (v: number) => number;
    static precision(n: number): (v: number) => number;
}
