export declare class Uri {
    static isAbsolute(uri: string): boolean;
    static getDirectory(uri: string): string;
    static join(base: string, needle: string): string;
    static getPathExt(path: string): string;
    static pathExtToContentType(pathExt: string): string | null;
}
