export declare class Html5FileFetcher {
    /**
     * read data from File interface(HTML5 File API).
     */
    static fetch(file: Blob): Promise<{
        contentType: string;
        data: ArrayLike<number>;
    }>;
}
